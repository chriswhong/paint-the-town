import cities from 'frontend/utils/cities';
import { module, test } from 'qunit';

module('Unit | Utility | cities', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = cities();
    assert.ok(result);
  });
});
